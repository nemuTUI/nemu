FROM alpine

RUN apk add --no-cache bash git cmake make gcc musl-dev pkgconfig \
    openssl-dev ncurses-dev libusb-dev eudev-dev json-c-dev libxml2-dev \
    sqlite-dev libarchive-dev gettext-dev graphviz-dev dbus-dev
